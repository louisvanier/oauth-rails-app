<%= form_with(model: revenue_share, local: true) do |form| %>
  <% if revenue_share.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(revenue_share.errors.count, "error") %> prohibited this revenue_share from being saved:</h2>

      <ul>
      <% revenue_share.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :amount %>
    <%= form.text_field :amount %>
  </div>

  <div class="field">
    <%= form.label :share_due %>
    <%= form.text_field :share_due %>
  </div>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
